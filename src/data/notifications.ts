import type { AdminNotification } from "@/types/notification";

// Mock notifications seeded from existing orders data
// These represent "new order" admin notifications
export const mockNotifications: AdminNotification[] = [];